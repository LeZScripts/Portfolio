# Nova Chen portfolio

A MERN portfolio starter with a React/Vite frontend and Express/Mongoose contact API.

## Run locally

```bash
npm install
npm run dev
```

The client runs at `http://localhost:5173`. Add `MONGODB_URI` from `.env.example` to persist contact messages in MongoDB; without it, the API still validates and accepts submissions for local demos.
